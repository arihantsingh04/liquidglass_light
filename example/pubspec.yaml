name: liquidglass_light_example
description: Demonstrates how to use the liquidglass_light package.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # DEPENDENCY MUST MATCH PACKAGE NAME
  liquidglass_light:
    path: ../