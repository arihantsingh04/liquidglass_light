name: liquidglass_light
description: A high-performance Flutter package that creates liquid, glass-like borders with gyro-simulated light reflections and dynamic gradients.
version: 0.0.1
homepage: https://github.com/your_username/liquidglass_light # REPLACE THIS
repository: https://github.com/your_username/liquidglass_light # REPLACE THIS
issue_tracker: https://github.com/your_username/liquidglass_light/issues # REPLACE THIS

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^6.0.0 # Updated to latest stable

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: